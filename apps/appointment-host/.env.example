# API base URL (use /api when behind nginx proxy to backend)
VITE_API_URL=/api

# Set to false to use live backend routes
VITE_USE_MOCKS=true

# Dev proxy target for /api
VITE_API_PROXY=http://localhost:5000
